[tool.poetry]
name = "doxygen_ai"
version = "0.1.0"
description = "Crew minima per test"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.13"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"